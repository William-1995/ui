{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "baseBranches": [
      "main"
    ],
    "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "matchDatasources": ["npm"],
      "minimumReleaseAge": "2 days"
    },
      {
          "matchDatasources": ["cdnurl"],
          "registryUrls":["https://cdn.sheetjs.com"]
      }
  ],
  "regexManagers": [
    {
        "fileMatch": [
            "^.*a.txt$"
        ],
        "matchStrings": [
          "renovate: datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?( packageName=(?<packageName>.*?))?( registryUrl=(?<registryUrl>.*?))?( extractVersion=(?<extractVersion>.*?))?\\s(0.20.1)(?<currentValue>.*?)\"?\\s"
        ]
    }
  ]
}
